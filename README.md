\# A simple Snake game built with Python's \`turtle\` module. Use the arrow
keys to move, eat the food, and avoid hitting the walls or yourself.

\## How to Run, Run \`main.py\` with Python 3: \`\`\`bash python main.py
